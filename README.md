# iglogin
```
Nowadays Instagram limit attempted login, the C version is useless until changes
Rely on the bash one
```
```
Single threaded brute with session saver in shellscript(bash)
<torport> must be an open port, script will bind a tor circuit through it
CTRL+C for exiting
$ bash iglogin <username> <wordlist> <torport>
```

```
Multi threaded brute with session saver in C
Bugged and/or not finished, here only for backup
```
